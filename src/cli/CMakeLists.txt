set(Target_Name "configdb-cli")
file(GLOB_RECURSE configdb_SRCS *.cpp *.h)
add_executable(${Target_Name} ${configdb_SRCS} )
target_link_libraries(${Target_Name} PRIVATE lib api)
set_target_properties(${Target_Name} PROPERTIES OUTPUT_NAME configctl)
install(TARGETS ${Target_Name} DESTINATION bin)
